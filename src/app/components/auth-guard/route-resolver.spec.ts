import { RouteResolver } from './route-resolver';
/*
describe('RouteResolver', () => {
  it('should create an instance', () => {
    const newLocal = new RouteResolver();
    expect(newLocal).toBeTruthy();
  });
});
*/